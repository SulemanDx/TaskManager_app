/**
 *  This will contain: 
 *   - All the querys that are use to interact with TaskDB
 */

// Define querys here:
const querySelectAllTasks = "SELECT * FROM tasks";

// Export the querys as an obj.
module.exports = {
    querySelectAllTasks
}